
#include <boost/multi_array.hpp>
#include <iostream>

int main() {
#if defined(METABENCH)
	using namespace boost;
    using Tcontainer = multi_array<double, <%= n %>>;
    Tcontainer container{boost::extents<%='[10]' * n%>};

    for (int i = 0; i < 10; ++i) {
        container[i]<%="[0]" * (n-1)%> = 1.0;
    }

    std::cout << container.num_elements() << std::endl;

  
#endif
}